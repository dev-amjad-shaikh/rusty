[package]
name = "rusty-store-conformance"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Executable conformance suites for every Rusty store trait"

[dependencies]
rusty-agent-runtime = { path = "../rusty-core" }
rusty-store = { path = "../rusty-store" }
async-trait = "0.1"
serde_json = "1"
tokio = { version = "1", features = ["full"] }

[dev-dependencies]
tempfile = "3"
